import React from 'react';
import { EmailForm, Footer } from '../../utils';

const Contact = () => {
    return (
        <main>
            <EmailForm />
            <Footer />
        </main>
    );
};

export default Contact;