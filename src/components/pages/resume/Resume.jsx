import React from 'react';
import { Footer } from '../../utils';

const Resume = () => {
    return (
        <main>
            <h1>Greetings from Resume</h1>
            <Footer />
        </main>
    );
};

export default Resume;